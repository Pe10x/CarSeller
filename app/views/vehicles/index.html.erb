<center><h1>Vehiculos</h1></center>

<table class="table table-hover table striped">
  <thead>
  <tr>
    <th>Nombre</th>
    <th>Marca</th>
    <th>Vehiculo</th>
    <th>Cliente</th>
    <th>Precio</th>
    <th>Kilometros</th>
    <th>Combustible</th>
    <th>Año</th>
  </tr>
</thead>

<tbody>
<% @vehicles.each do |vehicle| %>
<tr class="success">
  <%#<tr class="vehicle<%#= cycle :succes, :error, :warning, :info%>
    <td><%= vehicle.name%></td>
    <td><%= vehicle.brand.name%></td>
    <td><%= vehicle.vehicle_type.name%></td>
    <td><%= vehicle.customer.name %></td>
    <td><%= vehicle.price%></td>
    <td><%= vehicle.kilimeters_covered%></td>
    <td><%= vehicle.fuel_type%></td>
    <td><%= vehicle.year%></td>
    <td><%= link_to 'Show', vehicle %></td>
    <td><%= link_to 'Edit', edit_vehicle_path(vehicle) %></td>
    <td><%= link_to 'Destroy', vehicle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</tbody>
</table>

<br />

<%= link_to icon('plus')+'Nuevo Vehiculo', new_vehicle_path, class: 'btn btn-success' %>
